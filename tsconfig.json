{
  "//": "Type-checking only — nothing is emitted. The codebase is plain JS with JSDoc types, checked by `npm run typecheck`.",
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "jsx": "react-jsx",
    "allowJs": true,
    "checkJs": true,
    "noEmit": true,
    "strict": false,
    "skipLibCheck": true,
    "resolveJsonModule": true,
    "allowImportingTsExtensions": false,
    "isolatedModules": true,
    "forceConsistentCasingInFileNames": true
  },
  "//include": "src/sim is fully typed. src/ui/EcuLab.jsx was one large untyped component and stayed excluded until the screen split; now that the split is done it no longer carries @ts-nocheck and is listed below like everything else built during the overhaul — tokens, primitives, screens — which was typed from the start.",
  "include": [
    "src/sim",
    "src/ui/tokens.js",
    "src/ui/theme.js",
    "src/ui/primitives",
    "src/ui/screens",
    "src/ui/state",
    "src/ui/EcuLab.jsx",
    "src/storage.js",
    "src/version.js",
    "src/globals.d.ts",
    "src/css-modules.d.ts",
    "tests",
    "scripts"
  ],
  "//exclude": "`exclude` only filters `include` — it does not stop tsc following an import. Nothing under src/ui opts out of typechecking via `@ts-nocheck` any more.",
  "exclude": ["node_modules", "dist"]
}
